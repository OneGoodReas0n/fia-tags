import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tags-component',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
